# launch redis first because the other containers depend on it 
docker run -d --network webber --name redis -v $(pwd)/data:/data redis
docker run --network webber -d --name server -p 8000:8000 server
